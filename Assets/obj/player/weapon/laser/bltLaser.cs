using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class bltLaser : bullet
{
    // Start is called before the first frame update
    void Start()
    {
        name = "playerLaser";
        damage = 15;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
