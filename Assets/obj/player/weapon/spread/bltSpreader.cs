using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
public class bltSpreader : bullet
{

    // Start is called before the first frame update
    void Start()
    {
        name = "playerSpread";
        damage = 50;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
