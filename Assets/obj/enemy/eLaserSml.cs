using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class eLaserSml : bullet
{
    
    // Start is called before the first frame update
    void Start()
    {
        bulletSpd = 1000;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
